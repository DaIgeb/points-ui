import * as fromTours from './tour';
import * as fromPeople from './person';
import * as fromRoutes from './route';

export * from './auth';

export const tours = fromTours;
export const routes = fromRoutes;
export const people = fromPeople;
